#include <iostream>

using namespace std;

// 学生クラスの定義
class Student
{
private:
  // 名前
  // 年齢
  // 学籍番号（整数型）

public:
  // データをセットするメンバ関数
  void setData()
  {
  }

  // データを出力するメンバ関数
  void displayData()
  {
  }

  // 年齢を取得するメンバ関数
  int getAge()
  {
  }

  // 学生データを入れ替えるためのコピー演算
  void copyFrom()
  {
  }
};

// バブルソートの実装
void bubbleSort()
{
}

int main()
{
  int NUM_STUDENTS; // 学生の人数
  cin >> NUM_STUDENTS;

  Student students[NUM_STUDENTS]; // 学生情報を格納する配列

  // 学生のデータを入力から格納する
  for (int i = 0; i < NUM_STUDENTS; ++i)
  {
  }

  // 関数bubbleSortを用いて学生のデータをソートする

  // ソートした後のデータを憑依
  for (int i = 0; i < NUM_STUDENTS; i++)
  {
  }

  return 0;
}
